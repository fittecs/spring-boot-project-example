spring:
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:${MYSQL_PORT:3306}/sbpe
    username: root
    password: pass
    initialization-mode: always
    type: com.zaxxer.hikari.HikariDataSource
    hikari:
      maximum-pool-size: 10
      connection-timeout: 1000
  jpa:
    properties:
      hibernate:
        show_sql: true
bigtable:
  project-id: ${BIGTABLE_PROJECT_ID:emulator}
  instance-id: ${BIGTABLE_INSTANCE_ID:test-instance}